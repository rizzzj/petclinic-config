apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: petclinic
  namespace: myapp       
spec:
  interval: 5m                 
  chart:
    spec:
      chart: ./app          
      sourceRef:
        kind: GitRepository
        name: petclinic-dev
        namespace: myapp
  valuesFrom:
    - kind: GitRepository
      name: petclinic-dev
      path: ./app/env/prod.yaml

